After installing R in a new machine, download these libraries below for gespatial works

library(sp)

library(classInt) #to make custom cuts in colormap

library(raster)

library(rasterVis)

library(rgdal)   #for spTransform

library(gdalUtils)

library(graphics)

library(mapproj)

library(maptools)  #for shapefiles

library(scales)  #for transparency

library(Cairo)

library(xlsx)

library(ncdf4)

library(gplots)

library(ggplot2)

library(filesstrings)

library(gtools)

library(devtools)

library(plot3D)

library(data.table)

library(R.matlab)

library(IDPmisc)

# Installing
install.packages("readr")

# Loading
library("readr") #The readr package contains functions for reading i) delimited files, ii) lines and iii) the whole file.

#--- Colormap Libraries ----#

library(colorspace)
library(colorRamps)
library(RColorBrewer)
library(wesanderson)
library(viridis)
library(dichromat)
#----------------------------